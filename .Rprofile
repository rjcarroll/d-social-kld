source("renv/activate.R")

# Raise the macOS R vector memory cap (default 16 GB is too small to serialize
# a large CmdStanR fit object). "unlimited" defers to OS-level limits.
mem.maxVSize("unlimited")
